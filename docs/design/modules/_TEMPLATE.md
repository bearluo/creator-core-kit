---
模块: <module-name>
所在包: packages/core | packages/engine | packages/tools
状态: 草案 v0          # 草案 → 评审中 → 已定稿 → 已实现
摘要: <一句话：本模块是什么。扫这句即可判断相关性>
何时读: <什么任务该打开本文>
日期: YYYY-MM-DD
依赖: <本模块依赖的其他模块/接口，或 无>
---

# <模块名> 设计文档

> 复制本模板到 `packages/<pkg>/docs/modules/<module>.md`（模块设计文档随包，见 CLAUDE.md「文档维护约定」）后填写。删除本引用行。

## TL;DR

<3–5 行：本模块解决什么、关键 API 一瞥、给谁用。让读者 / AI 不读全文也能拿到要点；细节见下。>

## Purpose（目标与定位）

- 这个模块做什么、解决什么问题、边界在哪。
- **定位/取舍**：与相邻模块的关系（补充还是取代）、是否可选、零依赖要求。
- **YAGNI**：首版故意砍掉哪些能力、留给后续。

## Public API（TypeScript 精确签名）

```ts
// 对外暴露的精确签名 + 关键注释（前置条件、副作用、错误语义）
```

## Behavior & data flow（行为与数据流）

- 内部数据结构；关键流程分步说明；重入 / 生命周期 / 边界情况。
- 若涉及 `cc`：明确哪部分在 core（纯逻辑）、哪部分在 engine（适配壳）、经哪个接口交互。

## Key design decisions（决策表）

| # | 维度 | 选项 | 推荐默认 | 一句话理由 |
|---|---|---|---|---|
| 1 |  |  |  |  |

## Platform considerations（全平台 / 小游戏兼容）

- 原生（iOS/Android/PC）/ Web / 微信·抖音小游戏 的差异与约束。
- 与三种"热"（线上热更 / 运行时分包 / 开发期热重载）的关系（如适用）。

## Testable seams + test plan（可测接缝 + vitest 用例）

- **可测性**：core 部分不得 import `cc`；引擎依赖通过哪个接口 mock。
- **用例清单**：逐条列出规划的 vitest 断言（正常路径 / 边界 / 错误 / 重入等）。

## Open Questions（待用户拍板）

1.

---

## 已知行为与坑（实现后补）

> ⚠️ 本节写**当前系统的真实行为**——踩过的坑、反直觉的时序、未覆盖的分支。
> **不写**过程流水（与设计偏差、commit、v1→v2 沿革）：那些属 `docs/design/…-proposal.md`、`docs/adr/` 与 git 历史。模块文档是现状，不是编年史。

- **反直觉行为 / 坑**：
- **未覆盖（ponytail / YAGNI）**：
- **当前限制**：
